# Unified Python deps for the entire repo (managed with uv).
# Install:  uv venv && uv pip install -r requirements.txt

# ── Workshop API (apps/workshop/api) ─────────────────────────────
fastapi>=0.128.0
uvicorn[standard]>=0.40.0
python-multipart>=0.0.21
python-dotenv>=1.0.0
httpx>=0.28.1
azure-identity>=1.17.0

# ── SOV demo notebooks + asset generation (demo/sov) ─────────────
azure-ai-contentunderstanding>=1.1.0
openpyxl>=3.1.2
reportlab>=4.0.0
Pillow>=10.0.0
img2pdf>=0.5.0
ipykernel>=6.29.0
pandas>=2.2.0
tabulate>=0.9.0

# ── Source-doc auditing (used during ground-truth alignment) ─────
pdfplumber>=0.11.0

# ── Document Intelligence custom classifier demo (demo/classifier) ─
azure-ai-documentintelligence>=1.0.0
azure-storage-blob>=12.20.0
